import toAStr from 'dummy/utils/to-a-str';
import { module, test } from 'qunit';

module('Unit | Utility | to a str');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toAStr();
  assert.ok(result);
});
