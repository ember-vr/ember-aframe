import components from 'dummy/utils/components';
import { module, test } from 'qunit';

module('Unit | Utility | components');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = components();
  assert.ok(result);
});
