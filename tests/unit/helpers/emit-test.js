
import { emit } from 'dummy/helpers/emit';
import { module, test } from 'qunit';

module('Unit | Helper | emit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emit([42]);
  assert.ok(result);
});

