
import { toAStr } from 'dummy/helpers/to-a-str';
import { module, test } from 'qunit';

module('Unit | Helper | to a str');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toAStr([42]);
  assert.ok(result);
});

