{{yield (hash
  header=(component "toggle-list-header"
    shouldHideToggle=@shouldHideToggle
    isExpanded=this.isExpanded
    toggle=(fn (mut this.isExpanded))
  )
  body=(component "toggle-list-body"
    isExpanded=this.isExpanded
  )
)}}