{{#toggle-list as |components|}}
  <h2>
    {{#components.header}}
      Default A-Frame Attributes (Ember Bound Attributes)
    {{/components.header}}
  </h2>
  {{#components.body}}
    <ul>
      {{#each defaultAttributes as |attribute|}}
        <li>
          {{attribute}}
        </li>
      {{/each}}
    </ul>
  {{/components.body}}
{{/toggle-list}}